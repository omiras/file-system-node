// Cargo el módulo interno 'fs'
const fs = require('fs');

// Cargo el módulo interno 'path' para construir rutas de forma más sencilla`
const path = require('path');

// Construye una ruta 
const pathToFile = path.join(__dirname, "files", "2abril_participants_94849208356.csv");